[package]
name = "composable"
version = "0.6.0"
edition = "2021"

readme = "README.md"
license = "MIT OR Apache-2.0"
include = ["LICENSE-APACHE.md", "LICENSE-MIT.md"]

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
futures = "0.3.29"
flume = { version = "0.11.0", features = ["select"], default-features = false }

[dependencies.derive_more]
version = "0.99.17"
features = ["from", "try_into", "as_ref", "as_mut"]
default-features = false

[dev-dependencies]
ntest_timeout = "0.9.0"


[profile.release]
lto = "thin"
#debug = true

[profile.deployment]
inherits = "release"
codegen-units = 1
incremental = false
lto = "fat"
strip = true

[profile.dev]
opt-level = 0

[profile.dev.package."*"]
opt-level = 3
